{
  "name": "hueworks-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "rm -rf dist && tsc --project tsconfig.json",
    "build:test": "rm -rf .test-dist && tsc --project tsconfig.test.json",
    "test": "npm run build && npm run build:test && node --test .test-dist/test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "24.6.0",
    "typescript": "5.9.3"
  }
}
