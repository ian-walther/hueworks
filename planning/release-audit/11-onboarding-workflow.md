# Chunk 11 (Phase 3): Guided Onboarding Workflow, Space Ignores, Test Stabilization

Status: complete; implementation reconciled 2026-07-26. Scope: `7a0fd54` (external-space ignores), `c07d08f` (SetupAreasLive, setup refactor, location UX, prefill hand-off, CSS), `f6ceb0f` (test stabilization), `MdnsTransport`. No open findings.

## Explicitly Fine / Leave-Alone

- **Mount-boundary refresh** (formerly OW-1, implemented): `SetupAreasLive` runs `refresh_designs()` once on connected mount plus an explicit per-bridge `retry_design`; decision events use the read-only design projection, with a regression test proving decisions write no external-space observations (`last_seen_at` unchanged). Both setup surfaces now share the same pattern.
- **Design failures render their reason** (formerly OW-2, implemented): a failed refresh shows the bounded `operation_error` with a bridge-specific retry action and unique panel ids; genuinely unfetched inventory keeps its separate guidance state.
- **Shared setup label helpers** (formerly OW-3, implemented): `HueworksWeb.SetupHelpers` owns `count_label`; both setup surfaces import it and deleted their diverging locals.
- **External-space ignores model** (`7a0fd54`): mapping and ignore are mutually exclusive via transactions that delete the other record before writing; `resolution/1` dispatches correctly across preloaded, nil, and `NotLoaded` association states; `skip_space` auto-resolves the parent floor when all children are decided, matching the migration's backfill heuristic. The moduledoc contract ("durable external-space resolutions… entity placement never changed here") holds in code.
- **Migration `20260719120000`**: additive, genuinely reversible `down`, testable SQL helper, and a backfill that reinterprets the old implicit "keep separate" state as an explicit floor ignore — commented as such, tested, and a no-op on the production DB (zero `external_spaces` rows per the runbook).
- **SetupAreasLive UX**: work-queue design (decided items leave the queue, completed floors collapse), progress meter with real `role="progressbar"` aria, "durable configuration — leave and return" copy, `keep_parent_floor_open` preserving customizer state, two-path floor cards with an explicit ignore row, and (post-OB-3) a "Keep as <Area>" action on resolved floors.
- **Post-save continuation** (`save_bridge` → `/setup?refresh_ha_inventory=<id>` → connected-only validated auto-fetch → `/setup/areas`): non-destructive, async, honest notices.
- **Location UX**: browser geolocation via a permission-gated `phx-hook` with coordinates staged into the validated form; postal lookup with third-party disclosure (chunk 10); `return_to` allowlisted to `"setup"` — no open-redirect surface.
- **Transition-default changes** (750→500ms, brightness scaling on): both columns `NOT NULL` with DB defaults, so existing installs keep stored values; new defaults affect fresh installs only. Constants centralized in `SolarConfig`. Product call, correctly scoped.
- **`f6ceb0f` test stabilization**: fail-loud `ArgumentError` for `async: true` DB tests (SQLite single writer) structurally prevents the "Database busy" flake class; `mdns_lite` `skip_udp` keeps tests off multicast; endpoint URL config supports the OAuth controller tests.
- **`MdnsTransport`**: bounded response-collection against a monotonic deadline, socket closed in `after`, injectable backend — and now the single query path for all three discoveries via the shared parser (chunk 10).
